import {useState} from "react";

const Room = () => {

    const [reservationStatus, setReservationStatus] = useState<boolean>(false);

    return (
        <div>
            <h1>Room</h1>
        </div>
    )
}

export default Room